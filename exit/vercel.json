{
  "buildCommand": "cd exit && npm install && npm run build",
  "outputDirectory": "exit/dist",
  "framework": "vite",
  "installCommand": "cd exit && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

